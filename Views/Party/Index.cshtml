@* @model AccountingSuite.Models.Common.PaginatedList<AccountingSuite.Models.Master.Party>

<h2>Party List</h2>
<a class="btn btn-primary" asp-action="Create">Add Party</a>

<form asp-action="Index" method="get" class="mb-3">
    <div class="form-group">
        <label for="searchTerm" class="form-label">Search Party</label>
        <input type="text" id="searchTerm" name="searchTerm" class="form-control"
               placeholder="Enter Party Code or Name" value="@ViewData["CurrentFilter"]" />
    </div>
    <button type="submit" class="btn btn-primary mt-2">Search</button>
    <a asp-action="Index" class="btn btn-outline-secondary mt-2">Clear</a>
</form>

<div class="card-body">
    <div class="table-responsive text-nowrap">
        <table class="table table-striped table-bordered table-hover mt-3">
            <thead>
                <tr>
                    <th>Party Code</th>
                    <th>Name</th>
                    <th>Party Type</th>
                    <th>GSTIN</th>
                    <th>Email</th>
                    <th>Active</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var p in Model)
                {
                    <tr>
                        <td>@p.PartyCode</td>
                        <td>@p.Name</td>
                        <td>@p.PartyType</td>
                        <td>@p.GSTIN</td>
                        <td>@p.Email</td>
                        <td>@(p.IsActive ? "Yes" : "No")</td>
                        <td>
                            <button class="btn btn-sm btn-info" data-bs-toggle="modal" data-bs-target="#detailsModal" onclick="loadDetails(@p.PartyId)">Details</button>
                            <button class="btn btn-sm btn-secondary" data-bs-toggle="modal" data-bs-target="#editModal" onclick="loadEdit(@p.PartyId)">Edit</button>
                            <button class="btn btn-sm btn-danger" data-bs-toggle="modal" data-bs-target="#deleteModal" onclick="loadDelete(@p.PartyId)">Delete</button>

                        </td>
                    </tr>
                }
            </tbody>
        </table>
        @await Html.PartialAsync("_Pagination", Model)
    </div>
</div>

<div class="text-muted text-center fw-semibold">
    Showing @Model.StartRecord–@Model.EndRecord of @Model.TotalCount records
</div>

<!-- Details Modal -->
<div class="modal fade" id="detailsModal" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog modal-lg">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">Party Details</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
            </div>
            <div class="modal-body" id="detailsContent">
                <!-- Details content will be loaded here -->
            </div>
        </div>
    </div>
</div>

<!-- Edit Modal -->
<div class="modal fade" id="editModal" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog modal-lg">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">Edit Party</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
            </div>
            <div class="modal-body" id="editContent">
                <!-- Edit form will be loaded here -->
            </div>
        </div>
    </div>
</div>

<!-- Delete Modal -->
<div class="modal fade" id="deleteModal" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">Delete Party</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
            </div>
            <div class="modal-body" id="deleteContent">
                <!-- Delete confirmation will be loaded here -->
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        // Ensure jQuery and Bootstrap are already included in _Layout before this section

        function loadDetails(id) {
            $("#detailsContent").load('/Party/Details/' + id, function (response, status) {
                if (status !== 'success') {
                    $("#detailsContent").html('<div class="text-danger">Failed to load details.</div>');
                }
            });
        }

        function loadEdit(id) {
            $("#editContent").load('/Party/Edit/' + id, function (response, status) {
                if (status !== 'success') {
                    $("#editContent").html('<div class="text-danger">Failed to load edit form.</div>');
                }
            });
        }

        function loadDelete(id) {
            $("#deleteContent").load('/Party/Delete/' + id, function (response, status) {
                if (status !== 'success') {
                    $("#deleteContent").html('<div class="text-danger">Failed to load delete confirmation.</div>');
                }
            });
        }

        // Delegated submit handler for forms loaded via .load()
        $(document).on('submit', '#editForm', function (e) {
            e.preventDefault();
            $.ajax({
                url: '/Party/Edit',
                type: 'POST',
                data: $(this).serialize(),
                headers: { 'RequestVerificationToken': getAntiForgeryToken() },
                success: function (result) {
                    if (result && result.success) {
                        location.reload();
                    } else {
                        $("#editContent").html(result); // re-rendered partial with validation errors
                    }
                },
                error: function () {
                    alert('Failed to save changes.');
                }
            });
        });

        $(document).on('submit', '#deleteForm', function (e) {
            e.preventDefault();
            $.ajax({
                url: '/Party/DeleteConfirmed',
                type: 'POST',
                data: $(this).serialize(),
                headers: { 'RequestVerificationToken': getAntiForgeryToken() },
                success: function (result) {
                    if (result && result.success) {
                        location.reload(); // refresh table
                    } else {
                        alert('Delete failed.');
                    }
                },
                error: function () {
                    alert('Failed to delete.');
                }
            });
        });

        // Read anti-forgery token from the page
        function getAntiForgeryToken() {
            return $('input[name="__RequestVerificationToken"]').val();
        }
    </script>
}
 *@

@model AccountingSuite.Models.Common.PaginatedList<AccountingSuite.Models.Master.Party>
@if (TempData["Message"] != null)
{
    <div class="alert alert-success alert-dismissible fade show mt-3" role="alert">
        @TempData["Message"]
        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
    </div>
}

@if (TempData["Error"] != null)
{
    <div class="alert alert-danger alert-dismissible fade show mt-3" role="alert">
        @TempData["Error"]
        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
    </div>
}


<div class="d-flex justify-content-between align-items-start mb-3">
    <!-- Left block: Party List + Add Party -->
    <div class="d-flex align-items-center gap-2">
        <h2 class="mb-0">Party List</h2>
        <a class="btn btn-primary" asp-action="Create">Add Party</a>
    </div>

    <!-- Right block: Search Party form -->
    <div class="w-50">
        <form asp-action="Index" method="get" class="mb-0 d-flex flex-column flex-grow-1">
            <div class="form-group d-flex align-items-center gap-2 mb-2">
                <label for="searchTerm" class="form-label mb-0 text-nowrap">Search Party</label>
                <input type="text" id="searchTerm" name="searchTerm" class="form-control border-dark"
                       placeholder="Enter keywords to get Party Details" value="@ViewData["CurrentFilter"]" />
                <div class="d-flex gap-2">
                    <button type="submit" class="btn btn-primary">Search</button>
                    <a asp-action="Index" class="btn btn-outline-secondary">Reset</a>
                </div>
             </div>
           
        </form>
    </div>
</div>


<div class="card-body">
    <div class="table-responsive text-nowrap">
        <table class="table table-striped table-bordered table-hover mt-3">
            <thead>
                <tr>
                    <th>Party Code</th>
                    <th>Name</th>
                    <th>Party Type</th>
                    <th>GSTIN</th>
                    <th>Contact Number</th> 
                    <th>Email</th>
                    <th>Active</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var p in Model)
                {
                    <tr>
                        <td>@p.PartyCode</td>
                        <td>@p.Name</td>
                        <td>@p.PartyType</td>
                        <td>@p.GSTIN</td>
                        <td>@p.ContactNumber</td>
                        <td>@p.Email</td>
                        <td>@(p.IsActive ? "Yes" : "No")</td>
                        <td>
                            <button class="btn btn-sm btn-info" data-bs-toggle="modal" data-bs-target="#detailsModal" onclick="loadDetails(@p.PartyId)">Details</button>
                            <button class="btn btn-sm btn-secondary" data-bs-toggle="modal" data-bs-target="#editModal" onclick="loadEdit(@p.PartyId)">Edit</button>
                            <button class="btn btn-sm btn-danger" data-bs-toggle="modal" data-bs-target="#deleteModal" onclick="loadDelete(@p.PartyId)">Delete</button>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
        @await Html.PartialAsync("_Pagination", Model)
    </div>
</div>

<div class="text-muted text-center fw-semibold">
    Showing @Model.StartRecord–@Model.EndRecord of @Model.TotalCount records
</div>

<!-- Modals (Details/Edit/Delete) remain unchanged -->
<!-- Details Modal -->
<div class="modal fade" id="detailsModal" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog modal-lg">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">Party Details</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
            </div>
            <div class="modal-body" id="detailsContent">
                <!-- Details content will be loaded here -->
            </div>
        </div>
    </div>
</div>

<!-- Edit Modal -->
<div class="modal fade" id="editModal" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog modal-lg">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">Edit Party</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
            </div>
            <div class="modal-body" id="editContent">
                <!-- Edit form will be loaded here -->
            </div>
        </div>
    </div>
</div>

<!-- Delete Modal -->
<div class="modal fade" id="deleteModal" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">Delete Party</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
            </div>
            <div class="modal-body" id="deleteContent">
                <!-- Delete confirmation will be loaded here -->
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        // Ensure jQuery and Bootstrap are already included in _Layout before this section

        function loadDetails(id) {
            $("#detailsContent").load('/Party/Details/' + id, function (response, status) {
                if (status !== 'success') {
                    $("#detailsContent").html('<div class="text-danger">Failed to load details.</div>');
                }
            });
        }

        function loadEdit(id) {
            $("#editContent").load('/Party/Edit/' + id, function (response, status) {
                if (status !== 'success') {
                    $("#editContent").html('<div class="text-danger">Failed to load edit form.</div>');
                }
            });
        }

        function loadDelete(id) {
            $("#deleteContent").load('/Party/Delete/' + id, function (response, status) {
                if (status !== 'success') {
                    $("#deleteContent").html('<div class="text-danger">Failed to load delete confirmation.</div>');
                }
            });
        }

        // Delegated submit handler for forms loaded via .load()
        $(document).on('submit', '#editForm', function (e) {
            e.preventDefault();
            $.ajax({
                url: '/Party/Edit',
                type: 'POST',
                data: $(this).serialize(),
                headers: { 'RequestVerificationToken': getAntiForgeryToken() },
                success: function (result) {
                    if (result && result.success) {
                        location.reload();
                    } else {
                        $("#editContent").html(result); // re-rendered partial with validation errors
                    }
                },
                error: function () {
                    alert('Failed to save changes.');
                }
            });
        });

        $(document).on('submit', '#deleteForm', function (e) {
            e.preventDefault();
            $.ajax({
                url: '/Party/DeleteConfirmed',
                type: 'POST',
                data: $(this).serialize(),
                headers: { 'RequestVerificationToken': getAntiForgeryToken() },
                success: function (result) {
                    if (result && result.success) {
                        location.reload(); // refresh table
                    } else {
                        alert('Delete failed.');
                    }
                },
                error: function () {
                    alert('Failed to delete.');
                }
            });
        });

        // Read anti-forgery token from the page
        function getAntiForgeryToken() {
            return $('input[name="__RequestVerificationToken"]').val();
        }
    </script>
}
